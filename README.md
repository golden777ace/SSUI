# SSUI

**SSUser Interface** — современная, сверхлёгкая и предельно быстрая GUI-библиотека для Windows, написанная с нуля на Rust с публичным API на Python.

> ⚠️ **Статус:** активная разработка (alpha). Нативное 2D-ядро на Rust работает, поверх него — декларативный Python-API через PyO3. Открывается окно, на GPU рисуется интерактивный интерфейс, доступны прозрачность и размытие фона.

---

## Что это

SSUI — попытка сделать то, чем должен был бы стать Tkinter сегодня: **простую в использовании** библиотеку с **огромным арсеналом настроек**, построенную на современном GPU-рендеринге вместо устаревшей софтверной отрисовки.

Два принципа, вокруг которых строится всё остальное:

1. **Простота использования.** Декларативный API, стилизация через CSS, разумные значения по умолчанию, четыре готовые темы «из коробки».
2. **Широкий спектр настроек.** Любой элемент — форма со свойствами (позиция, цвета, границы, тени, градиенты, прозрачность) и правилами взаимодействия. Любой цвет можно переопределить поверх темы.

## Что уже работает

### Ядро на Rust
- ⚡ **GPU-рендеринг** через Direct3D 11 + Direct2D + DirectWrite (flip-model swap chain, high-DPI).
- 🧱 **Дерево элементов** на аренном `Vec` + линейная раскладка (направление, отступы, gap, фиксированные и растяжимые размеры).
- 🎨 **Четыре темы:** white, light, dark, black (переключение пробелом), семантические токены.
- 🧩 **Виджеты:** `Frame`, `Label`, `Button`, `Slider`, `ProgressBar`, `Checkbox`, `TextBox`, `Dropdown`, `Tabs`, `Table`.
- ⌨️ **Ввод:** мышь (hover/press/drag, курсоры), клавиатура, фокус, навигация **Tab/Shift+Tab**, активация Enter/Space, колесо прокрутки, **IME**.
- 📝 **Редактор текста:** каретка, выделение мышью и Shift-стрелками, Home/End, Delete, Ctrl+A/C/V/X/Z/Y, буфер обмена, undo/redo, горизонтальная прокрутка.

### Оформление
- 🎨 **CSS-подмножество:** селекторы по типу/классу/`*`, псевдосостояния `:hover`/`:focus`, свойства фона, цвета, отступов, радиуса, переноса.
- 🌈 **Градиенты** с направлением (вертикаль, горизонталь, две диагонали).
- 🌑 **Тени/elevation** для панелей и кнопок.
- 🔤 **Перенос текста по словам** (`wrap`).
- 🫥 **Прозрачность:** альфа-канал в цветах (`#RRGGBBAA`) и настоящее прозрачное окно через DirectComposition.
- 🪟 **Размытие фона за окном** (glass/blur) с гашением на время перемещения ради плавности.

### Python-API (PyO3)
- 🐍 Декларативная композиция через `with`-контексты и краткие имена.
- ✳️ **Сигналы** и точечная реактивность (перерисовка привязок при изменении).
- 🎞️ **Анимации** (кривые, длительности, тикер 60 Гц).
- 🧰 **Диалоги, контекстное меню, инспектор раскладки (F12)**.
- 🎚️ Живое управление настройками окна (тон фона, режим размытия) из кода.

## В планах

- 🖼️ **Изображения** в Canvas (иконки, логотипы через WIC).
- 🧮 **Constraint-раскладка** в дополнение к линейной; кэш геометрии.
- 🎨 **Полный CSS-каскад**.
- 🧊 Позже — исследование **3D-интерфейсов**.

## Технологический стек

| Слой | Технология | Статус |
|------|-----------|--------|
| Ядро движка | Rust | ✅ |
| Окно и ввод | Win32 (`windows` crate 0.62) | ✅ |
| Рендеринг | Direct3D 11 + Direct2D + DirectWrite | ✅ |
| Прозрачность | DirectComposition + DWM blur | ✅ |
| Темы | 4 темы + семантические токены | ✅ |
| Публичный API | Python 3.11+ (PyO3, maturin) | ✅ |
| Стили | CSS (подмножество) | ✅ → полный ⏳ |
| Раскладка | линейная (есть) + constraint (планируется) | 🔄 |
| Изображения | WIC | ⏳ |

## Пример на Python

```python
import ssui

def main():
    vol = ssui.sgnl(0.0)

    win = ssui.W("Привет, SSUI", 640, 480, thm="drk", glass=True, blur=True)
    fx = win.fx()

    with win.bx(rad=16.0, pd=20.0, gp=12.0):
        win.lb(bind=lambda: f"Громкость: {int(vol() * 100)}%", h=28.0)
        win.pr(bind=lambda: vol(), h=20.0)
        win.sl(0.0, ch=lambda v: vol.st(v), h=36.0)
        win.bt("В 100%", h=44.0, clk=lambda: fx(vol, 1.0, dur=0.5))

    win.go()

if __name__ == "__main__":
    main()
```

## Демонстрации

- `python/examples/showcase.py` — витрина всех виджетов и механик.
- `python/examples/glass.py` — прозрачное стеклянное окно.
- `python/examples/blur.py` — стекло с размытием и регуляторами.

## Целевая платформа

- **ОС:** Windows 10/11 (пока только). Кроссплатформенность не в приоритете.
- **Устройства:** ПК.

## Сборка

```bash
python -m venv .venv
.venv\Scripts\activate
pip install maturin
maturin develop
python python/examples/showcase.py
```

## Дорожная карта

Подробный план — в [ROADMAP.md](./ROADMAP.md).

## Лицензия

[MIT](./LICENSE) © Sergievskiy Sergey

## Автор

Sergievskiy Sergey ([@golden777ace](https://github.com/golden777ace))